name: Manual Visual Confirmation

on:
  workflow_dispatch:

jobs:
  confirm:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm manual trigger
        run: echo "✅ Manual workflow executed successfully!"
