name: Main Branch Visual Confirmation

on:
  push:
    branches:
      - main

jobs:
  confirm:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm main branch update
        run: echo "✅ Workflow triggered by update to main branch!"
